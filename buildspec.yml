version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto11
  build: 
    commands:
      - chmod +x gradlew
      - ./gradlew build 
      - ls 
artifacts:
  files:
    - hello_world-0.0.1-SNAPSHOT.jar
    
